{
	"scripts": {
		"build": "rm -rf dist/* && tailwindcss build -i app.css -o dist/assets/styles/app.css -m && gleam export erlang-shipment && mv build/erlang-shipment dist",
		"dev": "ts-node scripts/watch.ts",
		"build:docs": "rm -rf docs && gleam docs build && mv build/dev/docs/chess_glm docs"
	},
	"devDependencies": {
		"tailwindcss": "^3.4.3"
	},
	"dependencies": {
		"chokidar": "^3.6.0",
		"ts-node": "^10.9.2"
	}
}
